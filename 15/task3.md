NULLクイズ

Q.quiz.phpの結果は？
Q.SELECT (money + friend) AS TOTAL FROM users;
Q.SELECT (money - friend) AS TOTAL FROM users;
Q.SELECT (money * friend) AS TOTAL FROM users;
Q.SELECT (money / friend) AS TOTAL FROM users;
